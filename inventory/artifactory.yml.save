# inventory/artifactory.yml
all:
  hosts:
    artifactory_server:
      ansible_host: 10.128.0.5  # Use the private IP or public IP as needed
      ansible_user: njekev65
      ansible_ssh_private_key_file: /path/to/your/private_key.pem  # Update with your SSH key path
